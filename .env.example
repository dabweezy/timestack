# Environment Variables for Timestack
# Copy this file to .env.local and update the values

# Application Settings
NODE_ENV=development
NEXT_PUBLIC_APP_NAME=Timestack
NEXT_PUBLIC_APP_VERSION=1.0.0

# Optional: Analytics (if needed in future)
# NEXT_PUBLIC_ANALYTICS_ID=

# Optional: API Settings (if backend is added)
# API_BASE_URL=http://localhost:3000/api
# API_SECRET_KEY=

# Optional: Database (if database is added)
# DATABASE_URL=

# Optional: Email Settings (if email features are added)  
# SMTP_HOST=
# SMTP_PORT=
# SMTP_USER=
# SMTP_PASS=

# Optional: File Storage (if file uploads are added)
# STORAGE_PROVIDER=local
# STORAGE_PATH=./uploads